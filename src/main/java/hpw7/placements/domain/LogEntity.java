package hpw7.placements.domain;

/**
 * Valid entities for log entries to be about.
 */
public enum LogEntity {
    APP_USER,
    AUTHORISATION_REQUEST,
    DOCUMENT,
    DURATION_TO_PROVIDER,
    LOG_ENTRY,
    PLACEMENT,
    PLACEMENT_APPLICATION,
    PLACEMENT_VISIT,
    PROVIDER
}
